# 高考志愿填报 Agent — 问题梳理与决策记录

## 1. 信息补全与关键信息记忆

**问题**：当前路由太刻板——用户必须一口气说全"辽宁理科600分"才触发推荐，缺任何一个字段就落入通用闲聊，回复"请告诉我省份、科类、分数"的模板话术。但用户实际对话是渐进的：

- 用户："我想学计算机" → agent 应识别到缺少省份/科类/分数，追问科类和分数，同时记住"计算机"
- 用户："辽宁的" → agent 应记住辽宁，继续追问科类和分数
- 用户："理科 600 分" → agent 合并记忆：辽宁+理科+600+计算机，触发推荐

**核心诉求**：
1. 每轮提取用户说了什么关键信息，不要丢弃
2. 跨轮累积记忆关键字段：省份、科类、分数、偏好专业、规避专业、偏好地区、规避地区、目标层次
3. 缺少必填字段时（省份/科类/分数），精准追问缺失项，不是甩一句通用模板
4. 信息齐全后自动触发推荐，不需要用户再确认

**补充：省份默认辽宁，拒绝其他省份**

- 省份不再是必填字段，默认辽宁
- 用户提到其他省份 → 明确告知："目前仅支持辽宁省，其他省份数据暂未覆盖"
- 这样必填字段只剩：**科类** + **分数**

**方案思路**：
- State 中新增 `user_profile` 字段，逐轮增量更新；province 初始值 = "辽宁"
- `route_node` 只做信息提取和合并，不直接判定意图
- 提取时识别到非辽宁省份 → 不更新 province，返回明确的能力边界提示
- `check_completeness`：科类 + 分数齐了 → 走推荐；缺字段 → 生成追问回复
- 追问要具体，如"请问你是物理类还是历史类？"而不是"请提供科类"



## 2. 意图覆盖不全

目前支持 recommend / school / major / search 四种。边界场景如"能上985吗""XX 和 YY 哪个好"未覆盖。

**回答**：边界场景确实无法穷举。策略是不追求意图全覆盖，而是**把 chat 兜底做好**：

- 四个核心意图（recommend / school / major / search）覆盖 80%+ 高频场景
- 其余一切未识别场景走 chat 兜底，但 chat 不能是现在这种"你好，请告诉我省份"的弱智模板
- chat 兜底时，LLM 基于 system prompt 里的报考常识 + 对话中已有的信息，给出有帮助的通用回答
- 如果确实需要查数据但现有工具做不到，坦诚告知局限性，不要假装查了
- 后续根据真实用户日志，发现高频未覆盖场景再针对性加意图，不提前设计

**原则**：`intent: chat` 不是失败，是合法的兜底路径——前提是它的回复质量够好。



## 3. 冲/稳/保分层是否合理

测试发现部分 42% 概率归入"冲"、98% 归入"稳"，分层逻辑可能有偏差。

**回答**：分层逻辑是后端推荐引擎的职责，不是 agent 的。agent 只管：
- 把用户信息传给 /recommend
- 把后端返回的冲/稳/保结果原样格式化输出
- 不自己重新分类、不调整概率、不篡改层次

如果分层有问题（如 42% 应该在稳却被标为冲），需要去后端推荐引擎那边修正。Agent 侧确保不引入额外偏差即可。



## 4. 会话存储：重启即丢失

目前 sessions 是内存字典，服务重启后对话历史全清空。

**回答**：需要持久化。方案：SQLite，只存关键信息，不存完整对话历史。

**存储字段**：
- `session_id` — 会话标识
- `province` — 省份（默认辽宁）
- `category` — 科类（物理/历史）
- `score` — 分数
- `rank` — 位次（后端返回后回填）
- `preferred_majors` — 偏好专业（JSON 数组）
- `avoid_majors` — 规避专业（JSON 数组）
- `preferred_regions` — 偏好地区（JSON 数组）
- `avoid_regions` — 规避地区（JSON 数组）
- `target_level` — 目标层次（985/211/一本等）
- `created_at` / `updated_at` — 时间戳

**原则**：只存结构化关键字段，不存原始对话 messages。轻量、够用。



## 5. 后端 API 可靠性与降级策略

/rank_query 曾返回 404。如果 /recommend 也挂掉，Agent 应如何处理。

**回答**：后端稳定性是后端的事，agent 侧只做降级兜底——后端不可用时，给用户通用报考建议而非直接报错。告诉用户"系统暂时无法获取最新数据，以下是一些通用建议"，然后基于对话中已有的省份/科类/分数信息，由 LLM 给出方向性指导。不编造具体学校数据即可。



## 6. DeepSeek 不原生支持 tool calling

V3（deepseek-chat）时期 bind_tools 静默失败、tool_choice 报错、structured_output 空模板，最终走手工 JSON。但已切到 V4-Pro，应该已支持原生 function calling，需要重新测试验证。

**回答**：V4 应该有了，抽时间用 `bind_tools` 重新测试一次。如果 V4 原生支持 OK，school / major / search 三个意图可以走标准 tool calling 路线，路由逻辑大幅简化。



## 7. streaming=True 全局浪费

路由层只需小段 JSON，不需要流式；回复层才需要。

**回答**：拆成两个 LLM 实例：

| 层 | 模型 | streaming | 用途 |
|----|------|-----------|------|
| 路由层 | `deepseek-v4-flash` | False | 信息提取、意图分类（轻量快速便宜） |
| 回复层 | `deepseek-v4-pro` | True | 生成冲/稳/保报告（质量优先） |

配合问题 1 的改造——推荐路径走正则，路由 LLM 只处理 school/major/search/chat 兜底，调用频率更低，flash 完全够用。



## 8. System Prompt 是否过度限制

当前 prompt 大量"禁止编造"规则。

**回答**：觉得当前 prompt 挺好，不需要放宽。保持严格的防编造约束。



## 9. 多轮对话的上下文理解

简单 messages 累积容易导致 LLM 注意力偏移、产生幻觉。

**回答**：后台维护一个**线性主线流程**，确保长对话不偏离用户核心目标：

**主线阶段**：
1. **信息收集** → 科类？分数？偏好？逐项追问补全
2. **生成推荐** → 信息齐全后触发 /recommend，输出冲/稳/保
3. **深度追问** → 针对推荐结果里的学校/专业深入分析
4. **调整优化** → 用户改偏好、改分数，重新推荐

**防偏移机制**：
- State 中维护 `main_goal`（当前所处阶段 + 核心参数），每轮都注入 system prompt
- 用户跳去问无关话题时，回答完轻轻拉回主线
- 不在 messages 里无限堆积历史，定期用 user_profile 结构化摘要替代原始对话



## 10. 部署运维：日志、监控、超时

暂不处理，先聚焦功能实现。


---

## 改动记录

| # | 文件 | 改动 | 状态 |
|---|------|------|------|
| 1 | `state.py` | 新增 `user_profile`（含 merge reducer）+ `stage` 字段 + `default_profile()` | ✅ |
| 2 | `config.py` + `.env` | 拆为 `deepseek_pro_model` + `deepseek_flash_model` 两个配置 | ✅ |
| 3 | `db.py` | 新建，SQLite 持久化 user_profile（10 个字段 + stage） | ✅ |
| 4 | `graph.py` | 完全重写 — 渐进式信息提取、正则 school/major/search 检测、双 LLM、阶段流转、缺失追问 | ✅ |
| 5 | `prompts/system.py` | 新增信息收集阶段引导、服务范围声明、降级策略 | ✅ |
| 6 | `main.py` | 对接 SQLite — lifespan 初始化、读写 session、DELETE 同步删除 | ✅ |

---

## 测试结果

| # | 场景 | 输入 | 预期 | 结果 |
|---|------|------|------|------|
| 1 | 信息不完整 | "我想学计算机" | 记住偏好，追问科类+分数 | ✅ |
| 2 | 渐进补全 | 接着"理科" | 记住物理类，追问分数 | ✅ |
| 3 | 信息齐全触发推荐 | 接着"600分" | 自动推荐，3322字符报告 | ✅ |
| 4 | 非辽宁拦截 | "北京理科600分" | 明确告知仅支持辽宁 | ✅ |
| 5 | 学校追问 | "东北林业大学怎么样" | 2214字符学校分析 | ✅ |
| 6 | 新会话问候 | "你好" | 默认辽宁，引导补全 | ✅ |
| 7 | SQLite 恢复 | 重启后 t1 追问 | 恢复全部 profile 并推荐 | ✅ |
| 8 | DELETE 清理 | DELETE /session/t1 | 内存 + SQLite 同步删除 | ✅ |
| 9 | 流式 | /chat/stream | 正常返回 [DONE] | ✅ |

---

## 第二轮改动：本地后端 + 位次查询

### 决策
- 后端从阿里云 (8.155.175.194:8002) 迁到本地 (127.0.0.1:8000)
- 恢复 `/rank_query` 调用：推荐前先查位次，把位次传给 /recommend
- 新增 `query_rank` 工具：用户可单独问"我这个分是什么位次"
- `/score_query`（位次转分数）端点接入 api_client，暂未暴露为独立工具

### 改动记录

| # | 文件 | 改动 |
|---|------|------|
| 7 | `.env` + `config.py` | BACKEND_API_URL → `http://127.0.0.1:8000` |
| 8 | `api_client.py` | 新增 `score_query()` 方法 |
| 9 | `tools/rank_query.py` | **新建** — 独立的位次查询工具 |
| 10 | `tools/recommend.py` | 推荐前先调 `/rank_query` 获取位次，传给 /recommend，结果中附带 rank_info |
| 11 | `graph.py` | 新增 `rank_query` 意图检测（正则匹配"位次/排名"关键词）+ tool_node 处理 |

### 测试结果

| 场景 | 输入 | 结果 |
|------|------|------|
| 位次查询接口 | GET /rank_query 辽宁物理600分 | ✅ 13322名，三年对比 |
| 渐进推荐 | "计算机" → "理科600分" | ✅ 3225字符推荐报告 |
| 问位次 | "我这个分数是什么位次" | ✅ 854字符位次回复 |

---

## 第三轮修复：推荐后重复推荐

**问题**：用户拿到推荐后问"XX院校不能报吗"，学校正则漏过 → 无匹配意图 → profile 已完整 → 再次触发推荐，返回同样的冲/稳/保报告。

**根因**：
1. 学校检测正则只覆盖 `怎么/如何/是什么/了解`，不覆盖 `能报吗/不能报吗/好吗/行不行`
2. 信息齐全后，任何未识别消息都走推荐，没有阶段保护

**修复**（`graph.py`）：
1. 学校正则扩展关键词：`能报/不能报/可以/好吗/好不好/推荐/怎么样/行不行/合适`，加裸学校名兜底匹配
2. 新增阶段保护：stage 已是 `recommending` 或 `deep_dive` 时，无显式意图走 chat → LLM 自然回复
3. `route_after_classify`：chat+reply=None → 路由到 respond，让 LLM 生成上下文感知回复
4. 图新增 `route → respond` 边

